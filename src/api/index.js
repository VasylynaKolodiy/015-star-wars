import {actors, actor} from "./drivers/actors"
import {starship, starships} from "./drivers/starships";


export const Api = {
  actors: actors,
  actor: actor,
  starships: starships,
  starship: starship,
};
